{

  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "npx tailwindcss -i ./src/input.css -o ./index.css --watch"
  },


  "devDependencies": {
    "tailwindcss": "^3.4.16"
  }
}
